---
import type { HTMLTag } from "astro/types";

interface Props {
  as?: HTMLTag;
  class?: string;
}

const { as: Tag = "div", class: className = "" } = Astro.props;
---

<Tag class={`section-copy ${className}`.trim()}>
  <slot />
</Tag>
